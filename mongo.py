# Arquivo obsoleto: persistência agora é feita em dados/notas.txt (JSONL)
# Não utilize mais este módulo. Todas as funções foram migradas para uso de arquivos.

raise ImportError('O sistema não utiliza mais MongoDB. Use apenas os arquivos locais para persistência.')